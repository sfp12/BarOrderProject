<!DOCTYPE html>
<html>
  <head>
    <title>ejs</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% if(names.length){ %>
      <ul>
        <% names.forEach(function(name){ %>
          <li foo='<%= name %>'><%= name %></li>
        <% }) %>
      </ul>
    <% } %>
  </body>
</html>
